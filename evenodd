#include<stdio.h>

int evenodd(int n)
{
    if(n%2==0)
    {
        printf("its a even number");
    }
    else
    {
        printf("its a odd number");
    }
}
int main()
{
    int a ;
    printf("Enter a number: ");
    scanf("%d",&a);
    evenodd(a);
}
